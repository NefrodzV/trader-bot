export default {
    presets: [
        [
            '@babel/preset-env',
            {
                targets: 'node 14', // Adjust for your node version if necessary
            },
        ],
    ],
}
